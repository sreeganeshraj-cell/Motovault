# ── .NET ──────────────────────────────────────────────
bin/
obj/
*.user
*.suo
.vs/
*.swp

# EF Core migrations snapshot intermediate
# (keep Migrations/ folder but ignore its Designer files if generated)

# Published output
publish/
out/

# Secrets & local config
appsettings.Development.json
appsettings.Local.json
*.pfx
*.p12

# ── Node / React ──────────────────────────────────────
web/motovault-web/node_modules/
web/motovault-web/dist/
web/motovault-web/.env
web/motovault-web/.env.local
web/motovault-web/.env.*.local

# ── Uploads (user-generated content, not source) ──────
src/MotoVault.API/wwwroot/uploads/

# ── Database ──────────────────────────────────────────
db/*.sql
db/*.dump
*.db
*.sqlite

# ── MCP / Claude Code ─────────────────────────────────
.mcp.json

# ── OS & editors ──────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
*.iml
